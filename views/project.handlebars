<div class="card">
<div class="card-body">
<div class="text-center">
  <img src="{{image}}" alt="Card image cap" width="400" 
     height="500" /></p>
  <h2>{{name}}</h2>
  <p>Type: {{winetype}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    Price: {{needed_funding}}
  </p>
  <p>
    <button class="btn btn-primary">Leave a comment</button>
  </p>
</div>
</div>
</div>