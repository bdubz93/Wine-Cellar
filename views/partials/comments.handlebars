          <h3>Hello</h3>
          {{#each commentText as |comment|}}
                <h1>{{comment}}</h1>
            {{/each}}


<div class="card">
<div class="card-body">
<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
    <h2>Welcome, {{comment.id}}!</h2>
  </div>
</div>

          {{#each commentText as |comment|}}
                <h1>{{comment}}</h1>
            {{/each}}