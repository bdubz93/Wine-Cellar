          <h3>Hello</h3>
          {{#each projects as |comment|}}
            <div class="col-md-8">
                <h1>{{comment.comment}}</h1>
                <h1>{{comment}}</h1>
              <p>{{user.name}}</p>
              <p>{{comment.length}} on {{format_date date_created}}</p>
            </div>
            {{/each}}




  <script src="./js/comment.js"></script>