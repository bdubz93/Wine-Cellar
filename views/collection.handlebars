{{!--
<h2>Welcome, {{name}}!</h2>
<h2>Welcome, {{comment.id}}!</h2>
</div>
</div>

{{#if projects.length}}
<div class="project-list">
  <h3>Current Wines:</h3>
  {{#each projects as |project|}}
  <div class="card" style="width:400px">
    <img class="card-img-top" src="{{image}}" alt="Card image cap" width="150" height="200">
    <div class="card-body">
      <h5 class="card-title">{{project.name}}</h5>
      {{project.description}}
    </div>
    <div class="card-footer">
      <p class="card-text">Created on {{format_date project.date_created}}</p>
      <a href="/project/{{project.id}}">{{project.name}}</a>
    </div>
  </div>
</div>
{{/each}}
</div>
{{/if}} --}}

{{!-- <script src="./js/profile.js"></script> --}}

{{#if projects.length}}
{{!-- <div class="col-auto"> --}}
  <div class="flex-column">
    <div class="card-columns">
      {{#each projects as |project|}}
      <div class="card-bg card" style="width:400px">
        <div class="card-header bg-white">
        <img class="mx-auto d-block" src="{{image}}" alt="Card image cap" width="150" height="200" />
        </div>
        <div class="card-body card-text">
          <h5 class="card-title ">{{project.name}}</h5>
          {{project.description}}
        </div>
        <div class="card-footer">
          <p class="card-text">Created on {{format_date project.date_created}}</p>
          <div class="card-footer">
          <a href="/project/{{project.id}}" class="btn btn-primary mx-auto d-block">{{project.name}}</a>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
</div>
{{!-- </div> --}}
{{/if}} 
